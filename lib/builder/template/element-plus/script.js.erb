import {api} from '../../api/index';
import Settingsmenu from './Settingsmenu';

export default {
    components: {
        Settingsmenu,
    },
    <%= "#{@data_hook}," %>
    <%= "#{@mounted_hook}" %>
    <%= "#{@methods_hook}" %>
};